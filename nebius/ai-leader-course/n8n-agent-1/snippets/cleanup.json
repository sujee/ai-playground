{{ (() => { const raw = ($json.choices && $json.choices[0] && $json.choices[0].message && $json.choices[0].message.content) ? $json.choices[0].message.content : ''; let parsed = {}; try { const m = raw.match(/\{[\s\S]*\}/); parsed = JSON.parse(m ? m[0] : raw); } catch (e) {} const q = (($('Customer Query Webhook').first().json.body && ($('Customer Query Webhook').first().json.body.query || $('Customer Query Webhook').first().json.body.message)) || ''); const dep = ((parsed.department || 'other') + '').toLowerCase(); const allowed = ['support', 'sales', 'billing', 'other']; return JSON.stringify({ query: q, department: allowed.includes(dep) ? dep : 'other' }); })() }}